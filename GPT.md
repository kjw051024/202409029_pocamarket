# GPT UI Entry Point

Before creating or modifying UI, read `index.md` first.

Then read only the Component Contract files required for the current task.

Do not invent or add undefined Tokens, Components, design rules, values, variants, or states. Use only the currently defined system.
